---
// Slider component for project showcase
---

<!-- Slider Section -->
<section class="bg-white py-16 md:py-20">
  <div class="section-container">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        Referans Projelerimiz
      </h2>
      <p class="text-xl text-gray-600">
        Tamamladığımız başarılı projelerden örnekler
      </p>
    </div>
    
    <!-- Slider Container -->
    <div class="relative max-w-6xl mx-auto">
      <!-- Slides -->
      <div id="slider" class="overflow-hidden rounded-2xl shadow-2xl">
        <div id="slides" class="flex transition-transform duration-500 ease-in-out">
          
          <!-- Slide 1 -->
          <div class="min-w-full">
            <div class="relative h-[400px] md:h-[500px] bg-gradient-to-br from-gray-100 to-gray-200">
              <img src="/images/proje1.jpg" alt="Proje 1" class="w-full h-full object-contain" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
              <div class="absolute inset-0 flex flex-col items-center justify-center text-center p-8" style="display:none;">
                <div class="text-8xl mb-4">🏗️</div>
                <h3 class="text-3xl font-bold text-gray-800 mb-2">Şantiye Projesi</h3>
                <p class="text-gray-600 text-lg">500+ WC Kabini Teslimatı</p>
              </div>
            </div>
          </div>
          
          <!-- Slide 2 -->
          <div class="min-w-full">
            <div class="relative h-[400px] md:h-[500px] bg-gradient-to-br from-gray-100 to-gray-200">
              <img src="/images/proje2.jpg" alt="Proje 2" class="w-full h-full object-contain" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
              <div class="absolute inset-0 flex flex-col items-center justify-center text-center p-8" style="display:none;">
                <div class="text-8xl mb-4">🏭</div>
                <h3 class="text-3xl font-bold text-gray-800 mb-2">Fabrika Projesi</h3>
                <p class="text-gray-600 text-lg">Toplu WC Kabin Üretimi</p>
              </div>
            </div>
          </div>
          
          <!-- Slide 3 -->
          <div class="min-w-full">
            <div class="relative h-[400px] md:h-[500px] bg-gradient-to-br from-gray-100 to-gray-200">
              <img src="/images/proje3.jpg" alt="Proje 3" class="w-full h-full object-contain" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
              <div class="absolute inset-0 flex flex-col items-center justify-center text-center p-8" style="display:none;">
                <div class="text-8xl mb-4">🏢</div>
                <h3 class="text-3xl font-bold text-gray-800 mb-2">Kamu Binası</h3>
                <p class="text-gray-600 text-lg">Özel Tasarım WC Kabinleri</p>
              </div>
            </div>
          </div>
          
          <!-- Slide 4 -->
          <div class="min-w-full">
            <div class="relative h-[400px] md:h-[500px] bg-gradient-to-br from-gray-100 to-gray-200">
              <img src="/images/proje4.jpg" alt="Proje 4" class="w-full h-full object-contain" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex'">
              <div class="absolute inset-0 flex flex-col items-center justify-center text-center p-8" style="display:none;">
                <div class="text-8xl mb-4">🎯</div>
                <h3 class="text-3xl font-bold text-gray-800 mb-2">Özel Proje</h3>
                <p class="text-gray-600 text-lg">Kompakt Laminat Uygulaması</p>
              </div>
            </div>
          </div>
          
        </div>
      </div>

      <!-- Navigation Arrows -->
      <button id="prevBtn" class="absolute left-4 top-1/2 -translate-y-1/2 bg-white/90 hover:bg-white text-gray-800 p-3 rounded-full shadow-lg transition-all z-10">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
      </button>
      <button id="nextBtn" class="absolute right-4 top-1/2 -translate-y-1/2 bg-white/90 hover:bg-white text-gray-800 p-3 rounded-full shadow-lg transition-all z-10">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
      </button>

      <!-- Dots Navigation -->
      <div class="flex justify-center gap-2 mt-6">
        <button class="dot w-3 h-3 rounded-full bg-primary-600 transition-all" data-slide="0"></button>
        <button class="dot w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all" data-slide="1"></button>
        <button class="dot w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all" data-slide="2"></button>
        <button class="dot w-3 h-3 rounded-full bg-gray-300 hover:bg-gray-400 transition-all" data-slide="3"></button>
      </div>
    </div>
  </div>
</section>

<script>
  let currentSlide = 0;
  const slides = document.getElementById('slides');
  const dots = document.querySelectorAll('.dot');
  const totalSlides = 4;

  function showSlide(index: number) {
    if (slides) {
      slides.style.transform = `translateX(-${index * 100}%)`;
    }
    
    // Update dots
    dots.forEach((dot, i) => {
      if (i === index) {
        dot.classList.remove('bg-gray-300');
        dot.classList.add('bg-primary-600');
      } else {
        dot.classList.remove('bg-primary-600');
        dot.classList.add('bg-gray-300');
      }
    });
  }

  function nextSlide() {
    currentSlide = (currentSlide + 1) % totalSlides;
    showSlide(currentSlide);
  }

  function prevSlide() {
    currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
    showSlide(currentSlide);
  }

  // Event listeners
  document.getElementById('nextBtn')?.addEventListener('click', nextSlide);
  document.getElementById('prevBtn')?.addEventListener('click', prevSlide);

  // Dots navigation
  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      currentSlide = index;
      showSlide(currentSlide);
    });
  });

  // Auto-play every 5 seconds
  setInterval(nextSlide, 5000);
</script>

<style>
  .section-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  @media (min-width: 640px) {
    .section-container {
      padding: 0 1.5rem;
    }
  }
  
  @media (min-width: 1024px) {
    .section-container {
      padding: 0 2rem;
    }
  }
</style>
